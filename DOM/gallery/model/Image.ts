class GalleryImage {
    url: string;
    id: number;
    title: string;

    constructor(url: string, id: number, title: string) {
        this.url = url;
        this.id = id;
        this.title = title;
    }
}